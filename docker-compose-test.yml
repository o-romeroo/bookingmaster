# Docker Compose para ambiente de testes de aceitação (E2E)
# Usado durante a Etapa de Teste de Aceitação no pipeline CI/CD
# Porta: 8090 (isolado da produção que usa 8080)
# 
# Variáveis necessárias (passadas via Jenkins secrets):
#   - TEST_DB_ROOT_PASSWORD
#   - TEST_DB_NAME
#   - TEST_DB_USER
#   - TEST_DB_PASSWORD

services:
  # ================================
  # MariaDB para Testes
  # ================================
  mariadb-test:
    image: mariadb:11.2
    container_name: bookingmaster-mariadb-test
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${TEST_DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${TEST_DB_NAME}
      MYSQL_USER: ${TEST_DB_USER}
      MYSQL_PASSWORD: ${TEST_DB_PASSWORD}
    ports:
      - "3308:3306"
    volumes:
      - mariadb_test_data:/var/lib/mysql
    networks:
      - bookingmaster-network
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ================================
  # BookingMaster API para Testes
  # ================================
  bookingmaster-test:
    image: bookingmaster:${BUILD_NUMBER:-latest}
    container_name: bookingmaster-api-test
    restart: unless-stopped
    depends_on:
      mariadb-test:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: test
      DATABASE_URL: jdbc:mariadb://bookingmaster-mariadb-test:3306/${TEST_DB_NAME}?createDatabaseIfNotExist=true
      DATABASE_USERNAME: ${TEST_DB_USER}
      DATABASE_PASSWORD: ${TEST_DB_PASSWORD}
      PORT: 8080
    ports:
      - "8090:8080"
    networks:
      - bookingmaster-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

# ================================
# Networks - Usa a rede existente do bookingmaster
# ================================
networks:
  bookingmaster-network:
    external: true
    name: bookingmaster-network

# ================================
# Volumes
# ================================
volumes:
  mariadb_test_data:
    name: bookingmaster-mariadb-test-data
